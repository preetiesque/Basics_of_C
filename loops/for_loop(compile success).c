#include <stdio.h>

int main()
{
    int i = 0;
    for (;i<3;i++){
        printf(i);
    }
    //the value of i is initialised outside the for loop,hence we don't need to initialise it inside the for loop

    //the program will compile with warnings generated.overall the compilation will be successful
}
