#include <stdio.h>

int main()
{
    for(int p = 10;p > 0; ){
        p = p -1;
        printf("%d\n",p);
        //the program will run succesfully because the value has been decremented inside the loop
    }
}
